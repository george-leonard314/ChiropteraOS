# The greeter draws through DRM/EGL, so its account needs the device groups.
# Without them wlroots fails with "DRI2: failed to create screen" and the
# login screen stays black. greetd's own sysusers file creates the account.
m greeter video
m greeter render
m greeter input
m greeter seat
