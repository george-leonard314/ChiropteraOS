# One tick box holding the ChiropteraOS app list; off by default, so a plain
# install stays lean. The list itself is chiroptera-netinstall.yaml.
---
groupsUrl: file:///etc/calamares/chiroptera-netinstall.yaml
label:
    sidebar: "Apps"
    title: "Extra apps"
required: false
