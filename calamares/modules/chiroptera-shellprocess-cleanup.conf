# In the target, right after the live image is copied: strip what only the
# live session needs, before the initramfs is built.
---
i18n:
    name: "Turning the live image into an installed system"
dontChroot: false
timeout: 900
script:
    - command: "/etc/calamares/scripts/chiroptera-live-cleanup"
      timeout: 900
