# Editor lock files
.~lock.*#

# Subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

# makepkg output
pkgs/*/src/
pkgs/*/pkg/
pkgs/*/*.pkg.tar.*
pkgs/*/*.log

# makepkg bare git-source clones (named after the source, one line per package)
pkgs/chiroptera-shell/chiroptera-shell/
pkgs/chiroptera-dots/chiroptera-dots/
